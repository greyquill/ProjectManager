KV_REST_API_URL="https://welcome-gorilla-22990.upstash.io"
KV_REST_API_TOKEN="AVnOAAIncDI1MTcwZDMwZjk4ZDI0NjFkOTkzYTFjOWY3ZTNhMDgyNHAyMjI5OTA"


# Environment Mode
# Set to DEV for local development (uses pm/ folder, no KV calls)
# Set to PROD for production (uses KV/Redis storage)
# If not set, defaults to DEV when NODE_ENV=development
ENVIRONMENT=DEV

# Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3004


# KV/Redis Configuration (for production only)
# These are automatically used when ENVIRONMENT=PROD or VERCEL=1
# In local development (ENVIRONMENT=DEV), these are ignored to save your free tier quota
# Vercel will automatically set these in production
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

   USE_LOCAL_KV=true
   UPSTASH_REDIS_REST_URL=http://localhost:8080
   UPSTASH_REDIS_REST_TOKEN=local-dev-token