# Application Environment
NODE_ENV=development

# Environment Mode
# Set to DEV for local development (uses pm/ folder, no KV calls)
# Set to PROD for production (uses KV/Redis storage)
# If not set, defaults to DEV when NODE_ENV=development
ENVIRONMENT=DEV

# Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3004

# KV/Redis Configuration
# Production: Vercel automatically sets these when ENVIRONMENT=PROD or VERCEL=1
# Local KV: Set these to use local Redis via REST API proxy (see docker-compose.yml)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Local KV Development (optional)
# Set USE_LOCAL_KV=true to use local Redis instead of file-based storage
# Requires: docker-compose up (starts Redis + REST API proxy on localhost:8080)
# Local KV env vars (add to .env.local):
# USE_LOCAL_KV=true
# UPSTASH_REDIS_REST_URL=http://localhost:8080
# UPSTASH_REDIS_REST_TOKEN=local-dev-token-any-value

# API Configuration (if needed in future)
# API_BASE_URL=http://localhost:3000

